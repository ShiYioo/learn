# Frontend 路线图与编辑器核对

## 已验证

- 文档页：`http://127.0.0.1:5173/directions/frontend`，桌面端实测固定坐标图谱、主线实线、分支虚线、推荐/替代徽标及学习进度均正常。
- 编辑页：`/roadmap-editor`，实测 Vue Flow 画布、缩放控制、缩略图、节点选择面板、16px 网格吸附控件与本地开发环境的“保存到项目”入口均正常显示。编辑入口位于顶部“工具”菜单；编辑页按分类提供 26 份路线图数据的选择器。画布显示与吸附间距一致的 16px 网格线，不再自动向其他节点吸附；根/主线节点为 160×48，分支节点为 144×48，均为网格的整数倍。空白处左键拖拽框选、按住中键或空格加拖拽平移、右键新建/添加子节点，以及多选时的对齐、均分和删除工具，均使用 Vue Flow 的原生选择与平移机制实现。连线右键可以插入主线或分支节点，自动将原连线拆分为两段，也可删除连线；纯文本节点支持多行，不连线，也不计入学习进度。推荐/替代徽标可为每个节点分别设为左侧或右侧，旧 JSON 默认右侧。画布编辑历史保存在当前会话：`⌘/Ctrl + Z` 撤销，`⌘/Ctrl + Shift + Z` 或 `Ctrl + Y` 重做；输入框内保留系统原生文本撤销。方向键平移画布（Shift 加速，主键盘与小键盘 NumLock 关闭时的方向键通用），`+`/`-`（含小键盘）缩放，输入框内不拦截；`⌘/Ctrl +` 与 `⌘/Ctrl -` 保留浏览器缩放，供滚轮不可用的环境使用。
- 构建：`git diff --check && bun run build` 通过。

## 结构核对

- 发布图谱由 `.vitepress/roadmap-definitions.mjs` 注册；`src/roadmaps/` 已包含 26 份 JSON。Frontend 仍是当前唯一嵌入文档页的路线图，其余 JSON 是可编辑的本地骨架，待内容完善后再嵌入对应页面。完成进度及编辑草稿分别使用独立的 `localStorage` 键，不会写入发布数据。
- 文档页不加载 Vue Flow。编辑器由异步组件按路由分包；构建产物中 `roadmap-editor.md` 入口约 4 KB，编辑器专属 chunk 约 220 KB，普通文档访问不下载该 chunk。
- 本地保存接口只在 `vitepress dev` 中注册，只能按注册表写入对应的 `src/roadmaps/*.json`，并校验路线图 id、节点、边、坐标、唯一根节点及文本节点不可连线等约束；生产构建没有该接口，编辑器只提供本地草稿与导入/导出。

## 后续扩展

新增路线时，只需在 `.vitepress/roadmap-definitions.mjs` 注册页面与 JSON 文件；查看器、完成进度和连线绘制逻辑无需复制。

final result: passed
